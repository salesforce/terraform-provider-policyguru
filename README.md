# terraform-provider-policy-sentry

Build the provider

```shell
make build
```

Build and install the provider.

```shell
make install
```

Run example configuration
```shell
make terraform-demo
```
